class Solution {
  public:
int sumOfMatrix(int N, int M, const vector<vector<int>> &Grid) {
    int sum = 0;
    for (int i = 0; i < N; ++i) {
        for (int j = 0; j < M; ++j) {
            sum += Grid[i][j];
        }
    }
    return sum;

    }
};
