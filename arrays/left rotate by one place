class Solution {
public:
    void rotateArrayByOne(vector<int>& nums) {
        int n=nums.size();
        int last=nums[n-1];
        for(int i=0;i<n;i++){
            swap(nums[i],nums[i+1]);
        }
        last=nums[0]
        return nums;

    }
};
